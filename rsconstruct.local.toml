# Repo-specific overlay. Deep-merged over the canonical rsconstruct.toml.

# Templates live in tera.templates/ and render to the repo root. Configs are
# Lua (config/*.lua), covered by the canonical dep_auto list.
[processor.tera]
src_dirs = ["tera.templates"]

# This repo is uv-managed: uv.lock (generated by `uv lock`) is the pin file
# and pyproject.toml declares the dependencies, so requirements.txt must not
# be generated from import scanning.
[processor.requirements]
enabled = false
